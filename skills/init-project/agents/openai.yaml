interface:
  display_name: "Init Project"
  short_description: "Initialize customizable frontend and backend project scaffolds"
  default_prompt: "Use $init-project to initialize a full-stack project with sensible defaults and verified setup."
